name: Pull Request validation

on: [pull_request]


jobs:
  verell-app-contracts-ci:
    name: Run verell-app-contracts validation
    uses: ./.github/workflows/verell-app-contracts-ci.yaml

  verell-app-frontend-ci:
    name: Run verell-app-frontend validation
    uses: ./.github/workflows/verell-app-frontend-ci.yaml

